#include "main.h"
/**
 * _abs -function that computes  the absolute value of an it
 *@r: the value
 * Descripiton: the standard library provides a similar funct..
 *
 * Return: (0)
 *
 */
int _abs(int r)
{
	if (r >= 0)

		return (r);
	else
		return (r * -1);
}

#include <stdlib.h>
#include <time.h>
/* more headers goes there */
#include <stdio.h>

/* betty style doc for function main goes there */
/**

int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	/* your code goes there */
	if (a > 0)
		print("%d is positvie", n);
	else if (a==0)
		print("%d is zero",n);
	else
		print("%d is negative",n)
	return (0);
}
